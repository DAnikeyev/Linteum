<div class="pixelmanager">
    <div class="pm-header" @onclick="ToggleMobilePanel">
        @if (Canvas == null)
        {
            <span class="pm-title">Canvas not loaded</span>
        }
        else
        {
            <div class="pm-canvas-summary">
                <div class="pm-canvas-primary">
                    <span class="pm-title">@Canvas.Name</span>
                    <span class="pm-canvas-meta">Size: @Canvas.Width × @Canvas.Height</span>
                </div>
                <span class="pm-selected-chip @(ClickedPixel.HasValue ? string.Empty : "pm-selected-chip-empty")">@SelectedPixelSummary</span>
            </div>
        }
        @if (IsMobileLayout)
        {
            <button class="pm-mobile-toggle" @onclick="ToggleMobilePanel" @onclick:stopPropagation="true"
                    title="@(_isMobileExpanded ? "Collapse panel" : "Expand panel")">
                @(_isMobileExpanded ? "▾" : "▴")
            </button>
        }
    </div>

    <div class="pm-content @(_isMobileExpanded ? "" : "pm-content-collapsed")">
        @if (IsEconomyCanvas)
        {
            <div class="pm-economy" role="form" aria-label="Economy bidding">
                <h6>Economy</h6>
                <div class="pm-economy-row">
                    <span>Gold</span>
                    <strong>@Gold</strong>
                </div>
                <div class="pm-economy-row">
                    <span>Current price</span>
                    <strong>@CurrentPixelPrice</strong>
                </div>
                <div class="pm-economy-row">
                    <span>Minimum bid</span>
                    <strong>@MinimumBid</strong>
                </div>
                <div class="pm-field">
                    <label for="economyBidInput" class="form-label">Bid in gold</label>
                    <input id="economyBidInput"
                           type="number"
                           min="@MinimumBid"
                           class="form-control pm-bid-input"
                           @bind="_economyBidText"
                           @bind:event="oninput"
                           disabled="@(!ClickedPixel.HasValue)" />
                    @if (!string.IsNullOrEmpty(EconomyValidationMessage))
                    {
                        <div class="pm-bid-error">@EconomyValidationMessage</div>
                    }
                </div>
            </div>
        }

        @if (IsNormalCanvas)
        {
            <div class="pm-economy" role="status" aria-label="Normal mode daily quota">
                <h6>Normal Mode</h6>
                <div class="pm-economy-row">
                    <span>Daily limit (this canvas)</span>
                    <strong>@(_normalModeQuota?.DailyLimit ?? 100)</strong>
                </div>
                <div class="pm-economy-row">
                    <span>Used today</span>
                    <strong>@(_normalModeQuota?.UsedToday ?? 0)</strong>
                </div>
                <div class="pm-economy-row">
                    <span>Left today</span>
                    <strong>@NormalModeRemainingToday</strong>
                </div>
                <div class="pm-text-note">Normal mode supports single-pixel painting only, with a separate daily quota on each canvas. Brush and text tools are available on FreeDraw canvases.</div>
            </div>
        }

        <div class="pm-drawing">
            <h6>@DrawingSectionTitle</h6>

            @if (IsFreeDrawCanvas)
            {
                <div class="pm-tabs" role="tablist" aria-label="Drawing mode">
                    <button type="button"
                            class="pm-tab @GetToolTabClass(DrawingTool.Pixel)"
                            @onclick="() => SetActiveToolAsync(DrawingTool.Pixel)">
                        <span class="pm-tab-label">Pixel</span>
                    </button>
                    <button type="button"
                            class="pm-tab @GetToolTabClass(DrawingTool.Text)"
                            @onclick="() => SetActiveToolAsync(DrawingTool.Text)">
                        <span class="pm-tab-label">Text</span>
                        <span class="pm-tab-note">β experimental</span>
                    </button>
                </div>
            }

            <div class="pm-fields">
                @if (IsTextToolActive)
                {
                    <div class="pm-field">
                        <label class="form-label">Foreground Color</label>
                        <div class="pm-color-select">
                            <button type="button"
                                    class="pm-color-select-trigger"
                                    @onclick="ToggleTextForegroundMenu"
                                    aria-haspopup="listbox"
                                    aria-expanded="@_isTextForegroundMenuOpen">
                                <span class="pm-color-option-main">
                                    <span class="pm-color-swatch" style="@(GetColorSwatchStyle(SelectedTextForegroundColor?.HexValue))"></span>
                                    <span>@GetColorDisplayName(SelectedTextForegroundColor, "Select color")</span>
                                </span>
                                <span class="pm-color-select-caret">▾</span>
                            </button>

                            @if (_isTextForegroundMenuOpen && _colors != null)
                            {
                                <div class="pm-color-select-menu" role="listbox" aria-label="Foreground color options">
                                    @foreach (var color in _colors)
                                    {
                                        <button type="button"
                                                class="pm-color-select-option @(color.Id == _textForegroundColorId ? "selected" : string.Empty)"
                                                @onclick="() => SelectTextForegroundAsync(color.Id)">
                                            <span class="pm-color-option-main">
                                                <span class="pm-color-swatch" style="@(GetColorSwatchStyle(color.HexValue))"></span>
                                                <span>@GetColorDisplayName(color, color.HexValue)</span>
                                            </span>
                                        </button>
                                    }
                                </div>
                            }
                        </div>
                    </div>

                    <div class="pm-field">
                        <label class="form-label">Background Color</label>
                        <div class="pm-color-select">
                            <button type="button"
                                    class="pm-color-select-trigger"
                                    @onclick="ToggleTextBackgroundMenu"
                                    aria-haspopup="listbox"
                                    aria-expanded="@_isTextBackgroundMenuOpen">
                                <span class="pm-color-option-main">
                                    <span class="pm-color-swatch @(SelectedTextBackgroundColor == null ? "pm-color-swatch-transparent" : string.Empty)"
                                          style="@((SelectedTextBackgroundColor == null ? null : GetColorSwatchStyle(SelectedTextBackgroundColor.HexValue)))"></span>
                                    <span>@GetTextBackgroundLabel()</span>
                                </span>
                                <span class="pm-color-select-caret">▾</span>
                            </button>

                            @if (_isTextBackgroundMenuOpen)
                            {
                                <div class="pm-color-select-menu" role="listbox" aria-label="Background color options">
                                    <button type="button"
                                            class="pm-color-select-option @(!SelectedTextBackgroundColorId.HasValue ? "selected" : string.Empty)"
                                            @onclick="() => SelectTextBackground(null)">
                                        <span class="pm-color-option-main">
                                            <span class="pm-color-swatch pm-color-swatch-transparent"></span>
                                            <span>Transparent</span>
                                        </span>
                                    </button>

                                    @if (_colors != null)
                                    {
                                        @foreach (var color in _colors)
                                        {
                                            <button type="button"
                                                    class="pm-color-select-option @(color.Id == SelectedTextBackgroundColorId ? "selected" : string.Empty)"
                                                    @onclick="() => SelectTextBackground(color.Id)">
                                                <span class="pm-color-option-main">
                                                    <span class="pm-color-swatch" style="@(GetColorSwatchStyle(color.HexValue))"></span>
                                                    <span>@GetColorDisplayName(color, color.HexValue)</span>
                                                </span>
                                            </button>
                                        }
                                    }
                                </div>
                            }
                        </div>
                    </div>

                    <div class="pm-field">
                        <label for="textFontSizeSelect" class="form-label">Font Size</label>
                        <select id="textFontSizeSelect" class="form-select pm-select" value="@_selectedTextFontSize" @onchange="OnTextFontSizeChanged">
                            @foreach (var fontSize in TextFontSizes)
                            {
                                <option value="@fontSize">@fontSize px</option>
                            }
                        </select>
                    </div>

                    <div class="pm-field">
                        <label for="textContentInput" class="form-label">Text</label>
                        <textarea id="textContentInput"
                                  class="form-control pm-textarea"
                                  rows="4"
                                  maxlength="500"
                                  placeholder="Type text to draw..."
                                  value="@_textContent"
                                  @oninput="OnTextContentInput"
                                  @onkeydown="OnTextContentKeyDownAsync"></textarea>
                        <div class="pm-text-note">Text starts at the selected pixel and is applied in batches of up to 100 pixels. Press Ctrl+Enter to paint.</div>
                    </div>

                    <div class="pm-field pm-paint-wrap">
                        <button class="btn pm-paint-btn" @onclick="PaintTextClick" disabled="@PaintTextDisabled">Paint Text</button>
                    </div>
                }
                else
                {
                    <div class="pm-field">
                        <label class="form-label">Color Palette</label>
                        <div class="color-palette">
                            @if (_colors != null)
                            {
                                @foreach (var c in _colors)
                                {
                                    <div class="color-square @(c.Id == _selectedColorId ? "selected" : "")"
                                         style="background:@c.HexValue;"
                                         title="@(string.IsNullOrEmpty(c.Name) ? c.HexValue : c.Name)"
                                         @onclick="() => SelectColorAsync(c)">
                                    </div>
                                }
                            }
                        </div>
                    </div>

                    <div class="pm-field pm-paint-wrap">
                        <button class="btn pm-paint-btn" @onclick="PaintClick" disabled="@PaintDisabled">@PaintButtonText</button>
                    </div>

                    @if (IsFreeDrawCanvas)
                    {
                        @if (_activeTool == DrawingTool.Pixel)
                        {
                            <div class="pm-field pm-brush-wrap">
                                <div class="pm-brush-actions">
                                    <button type="button"
                                            class="btn pm-brush-toggle @(IsBrushEnabled ? "active" : string.Empty)"
                                            @onclick="ToggleBrushAsync"
                                            disabled="@BrushToggleDisabled"
                                            aria-pressed="@IsBrushEnabled"
                                            title="@BrushToggleTitle">
                                        Brush @(IsBrushEnabled ? "On" : "Off")
                                    </button>
                                    <button type="button"
                                            class="btn pm-brush-toggle pm-eraser-toggle @(IsEraserBrushEnabled ? "active" : string.Empty)"
                                            @onclick="ToggleEraserBrushAsync"
                                            disabled="@EraserToggleDisabled"
                                            aria-pressed="@IsEraserBrushEnabled"
                                            title="@EraserToggleTitle">
                                        Eraser @(IsEraserBrushEnabled ? "On" : "Off")
                                    </button>
                                </div>
                            </div>

                            @if (IsEraserBrushEnabled)
                            {
                                <div class="pm-field">
                                    <label class="form-label">Eraser Size</label>
                                    <div class="pm-eraser-sizes d-flex gap-2">
                                        @foreach (var size in EraserSizes)
                                        {
                                            <button type="button"
                                                    class="btn btn-sm btn-outline-secondary @(SelectedEraserSize == size ? "active" : string.Empty)"
                                                    @onclick="() => SetSelectedEraserSizeAsync(size)">
                                                @size x @size
                                            </button>
                                        }
                                    </div>
                                </div>
                            }
                        }
                    }
                }
            </div>
        </div>

        <div class="pm-creator-tools">
            @if (HasCanvasMaintenanceProgress)
            {
                <div class="pm-economy pm-maintenance-card" role="status" aria-live="polite" aria-label="Canvas maintenance status">
                    <h6>Canvas Operation</h6>
                    <div class="pm-maintenance-header">
                        <span class="pm-maintenance-badge @MaintenanceBadgeClass">@MaintenanceStatusLabel</span>
                        <span class="pm-maintenance-operation">@MaintenanceOperationLabel</span>
                    </div>
                    @if (!string.IsNullOrWhiteSpace(MaintenanceMessage))
                    {
                        <div class="pm-maintenance-message">@MaintenanceMessage</div>
                    }
                    <div class="pm-text-note">Updated @MaintenanceUpdatedLabel</div>
                </div>
            }

            @if (Canvas != null && (CanEraseCanvas || CanDeleteCanvas))
            {
                <div class="pm-economy pm-creator-card" aria-label="Canvas tools">
                    <h6>Canvas Tools</h6>
                    <div class="pm-creator-actions">
                        @if (CanEraseCanvas)
                        {
                            <button type="button" class="btn pm-creator-btn pm-creator-erase-btn" @onclick="PromptEraseCanvas" disabled="@(_isManagingCanvas || HasActiveCanvasMaintenance)">Erase canvas</button>
                        }
                        @if (CanDeleteCanvas)
                        {
                            <button type="button" class="btn pm-creator-btn pm-creator-delete-btn" @onclick="PromptDeleteCanvas" disabled="@(_isManagingCanvas || HasActiveCanvasMaintenance)">Delete canvas</button>
                        }
                    </div>
                    @if (HasPendingCanvasAction)
                    {
                        <div class="pm-canvas-confirmation" role="alertdialog" aria-live="assertive" aria-labelledby="pm-canvas-confirmation-title">
                            <div id="pm-canvas-confirmation-title" class="pm-canvas-confirmation-title">@PendingCanvasActionTitle</div>
                            <div class="pm-canvas-confirmation-message">@PendingCanvasActionMessage</div>
                            <div class="pm-canvas-confirmation-actions">
                                <button type="button" class="btn pm-creator-btn pm-canvas-confirmation-cancel" @onclick="CancelCanvasAction" disabled="@_isManagingCanvas">Cancel</button>
                                <button type="button"
                                        class="btn pm-creator-btn pm-canvas-confirmation-confirm @PendingCanvasActionButtonClass"
                                        @onclick="ConfirmCanvasActionAsync"
                                        disabled="@_isManagingCanvas">
                                    @PendingCanvasActionButtonText
                                </button>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>

        <div class="pm-online-users">
            <h6>Online Users (@OnlineUsers.Count)</h6>
            <div class="pm-user-list">
                @if (OnlineUsers.Any())
                {
                    @foreach (var user in OnlineUsers)
                    {
                        <span class="pm-user-chip">@user</span>
                    }
                }
                else
                {
                    <span class="pm-user-empty">Only you are here</span>
                }
            </div>
        </div>

        @if (_pixelChangeHistory != null && _pixelChangeHistory.Any())
        {
            <div class="pm-history">
                <h6>Pixel History</h6>
                <div class="history-list">
                    @foreach (var item in _pixelChangeHistory)
                    {
                        var oldColor = _colors?.FirstOrDefault(c => c.Id == item.OldColorId);
                        var newColor = _colors?.FirstOrDefault(c => c.Id == item.NewColorId);

                        <div class="history-item">
                            <div class="history-colors">
                                <div class="color-swatch-sm" style="background:@(oldColor?.HexValue ?? "transparent");" title="Old: @(oldColor?.Name ?? "?")"></div>
                                <span class="arrow">→</span>
                                <div class="color-swatch-sm" style="background:@(newColor?.HexValue ?? "transparent");" title="New: @(newColor?.Name ?? "?")"></div>
                            </div>
                            <div class="history-details">
                                <span class="username">@item.UserName</span>
                                <span class="timestamp">@item.Timestamp.ToLocalTime().ToString("g")</span>
                            </div>
                        </div>
                    }
                </div>
            </div>
        }
    </div>
</div>

